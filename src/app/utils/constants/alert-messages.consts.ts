import { AlertMessages } from '../interfaces/admin-panel.interfaces';

const success = 'Пост был создан';
const warning = 'Пост был обновлен';
const danger = 'Пост был удалён';

export const ALERT_MESSAGE: AlertMessages = {
  success,
  warning,
  danger
}
